#include<stdio.h>

int main() 
{
    int a,b,c,d,e;
    float sum, per;
    printf("enter the subject marks:");
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    sum= a+b+c+d+e
    per= (sum/500)*100
    if(per>=90&& per<=100)}
    {
        printf("GRADE A");
    }
    else if(per>=80&& per<90)
    {
        printf("GRADE B");
    }
}